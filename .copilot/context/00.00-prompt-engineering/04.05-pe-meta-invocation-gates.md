---
title: "PE-meta invocation gates — Phase 0a CF-05 and Phase 0b domain coherence"
description: "Single source of truth for the two deterministic pre-Phase-1 invocation gates enforced by every pe-meta-* orchestrator and per-artifact prompt: the Phase 0a CF-05 artifact-type/path consistency check and the Phase 0b domain coherence check (3-tier metadata-first resolution, seed-vs-deps decision matrix, dispatch table, bundle=… closed set, consent token, --skip rejection)"
domain: "prompt-engineering"
goal: "Define the algorithm and contract for the two deterministic invocation gates that every pe-meta-* prompt enforces between argument parsing (Phase 0/0a) and source research (Phase 1), so each prompt cites this file as upstream authority rather than inheriting from a sibling prompt"
scope:
  covers:
    - "Phase 0a CF-05 artifact-type/path consistency check — applicability rules, prompt-name-prefix → expected-root table, rejection message format"
    - "Phase 0b domain coherence check — purpose, inputs, 3-tier metadata-first resolution algorithm, seed footprint vs dependency footprint distinction, decision matrix, dispatch table, gate behavior summary"
    - "bundle=… closed-set dispositions and the bundle=accept consent token"
    - "First-line Resolved invocation: log marker contract"
    - "--skip domain-coherence CF-05 rejection rule"
    - "Per-prompt-class applicability matrix (orchestrator-level vs per-artifact vs delegated)"
  excludes:
    - "Orchestrator implementation choices (per-prompt Process sections own those)"
    - "Per-artifact prompt CF-05 root-pin tables beyond the canonical mapping (per-artifact prompts may pin their single expected root locally; the canonical mapping lives here)"
    - "Domain detection beyond the 3-tier algorithm (the upstream spec is vision v15 § Domain detection)"
boundaries:
  - "This file is the IMPLEMENTATION SoT for Phase 0a CF-05 + Phase 0b consumed by pe-meta-* prompts and per-artifact prompts. The UPSTREAM authority is the current vision document (see § References)."
  - "MUST be cited by every pe-meta-* orchestrator (pe-meta-review, pe-meta-create-update, pe-meta-design, pe-meta-adherence, pe-meta-scheduled-review, pe-meta-release-monitor) and every per-artifact prompt (pe-meta-{context|prompt|instruction|agent|template|skill|hook|snippet}-{review|create-update|design}) as the source of truth for these two gates."
  - "Sibling prompts MUST NOT cite each other for Phase 0a/0b — they MUST cite this file (peer-to-peer inheritance between orchestrators is an anti-pattern)."
  - "Algorithm changes are made HERE first, then propagated by re-running pe-meta-* adherence checks. The vision document MAY introduce or evolve these gates; this file MUST track vision updates."
rationales:
  - "Eliminates peer-to-peer coupling between sibling orchestrators (pe-meta-review citing pe-meta-create-update for an algorithm both implement is architecturally wrong — both are peer consumers of the same vision contract)"
  - "Aligns the six Wave-1+2 orchestrators on a single algorithm SoT so adherence rubrics (pe-meta-adherence R-Pa/R-Pb) have one location to verify"
  - "Keeps the vision document as the upstream authority for the contract; this file is the implementation-facing translation cited by prompts/agents"
  - "Honors PE artifact architecture: context files own algorithm/pattern SoT; prompts cite context, never each other"
---

# PE-meta invocation gates — Phase 0a CF-05 and Phase 0b domain coherence

**Purpose.** Single source of truth for the two deterministic pre-Phase-1 gates that every pe-meta-* orchestrator and per-artifact prompt enforces between argument parsing and source research.

**Upstream authority.** Vision v15 § Domain-coherent batching, § Domain detection, § Pipeline phases and `--skip` mapping. This file translates the vision contract into an implementation-facing specification cited by all prompts. When the vision changes, update this file FIRST and propagate via pe-meta-* adherence checks.

**Consumers.** All six pe-meta-* orchestrators (`pe-meta-review`, `pe-meta-create-update`, `pe-meta-design`, `pe-meta-adherence`, `pe-meta-scheduled-review`, `pe-meta-release-monitor`) and the 24 per-artifact prompts under `.github/prompts/00.09-pe-meta/`.

---

## 🎯 The two gates at a glance

| Gate | Concern | Determinism source | Applies to |
|---|---|---|---|
| **Phase 0a CF-05** | Did the user invoke a per-artifact prompt with a path under the WRONG artifact-type root? | Path string + prompt name | Per-artifact prompts ONLY (orchestrator-level prompts skip) |
| **Phase 0b** | Does the resolved scope (seed + `--deps` closure) span ≥ 2 semantic domains? | Each in-scope file's declared `domain:` frontmatter (3-tier fallback) | All pe-meta-* prompts (orchestrator-level AND per-artifact) |

Both gates run AFTER Phase 0 / 0a parsing and BEFORE Phase 1 source research. Both gates emit observable side effects on the first-line `Resolved invocation:` log so the disposition is auditable before any mutation. Neither gate is skippable through `--skip`.

---

## 🛡️ Phase 0a CF-05 — artifact-type/path consistency check

**When it runs.** At the END of Phase 0a, after free-form resolution has settled but BEFORE Phase 0b. Runs ONLY on per-artifact prompt invocations. Orchestrator-level prompts (`/pe-meta-review`, `/pe-meta-create-update`, `/pe-meta-design`, `/pe-meta-adherence`, `/pe-meta-scheduled-review`, `/pe-meta-release-monitor`) are artifact-type-agnostic by design and SKIP this check.

**What it does.** Compares the artifact-type ROOT implied by the prompt name against the artifact-type ROOT resolved from the positional `<file-path>` (or `--scope` value). Mismatch is REJECTED with CF-05 before Phase 0b runs.

**Prompt-name-prefix → expected-root table.**

| Prompt name prefix | Expected root |
|---|---|
| `pe-meta-context-*` | `.copilot/context/` |
| `pe-meta-instruction-*` | `.github/instructions/` |
| `pe-meta-agent-*` | `.github/agents/` |
| `pe-meta-prompt-*` | `.github/prompts/` |
| `pe-meta-skill-*` | `.github/skills/` |
| `pe-meta-hook-*` | `.github/hooks/` |
| `pe-meta-template-*` | `.github/templates/` |
| `pe-meta-snippet-*` | `.github/prompt-snippets/` |

**CF-05 rejection message format.**

```text
CF-05: artifact-type/path mismatch. Invoked prompt /<invoked-prompt> expects positional paths under <expected-root>; supplied path resolves under <actual-root>. Canonical replacement: /<canonical-prompt> '<supplied-path>' <other-args>.
```

**Operates on ROOT, not domain.** Deterministic from path. Does NOT read frontmatter. Does NOT make domain assertions — domain coherence is the next gate's concern.

**Why orchestrator-level prompts skip.** `/pe-meta-review`, `/pe-meta-create-update`, etc. accept paths to any artifact type (they route work via the per-artifact prompt invocation matrix). Enforcing CF-05 at the orchestrator layer would falsely reject legitimate routing.

---

## 🌐 Phase 0b — Domain coherence check

**Goal.** Prevent silent heterogeneous batching by computing the semantic-domain footprint of the resolved scope (seed AND `--deps`-closure) BEFORE Phase 1 runs. When the footprint exceeds one domain, gate `--mode apply` until the caller either splits the run per-domain or appends explicit `bundle=accept` consent. Honest per-file metadata is the input; deterministic dispatch is the output.

### Inputs

- The canonical eight-parameter invocation resolved by Phase 0a (and validated against the Phase 0a precondition CF-05 check when applicable).
- The **metadata-first 3-tier domain resolution algorithm** defined in vision v15 § Domain detection:
  - **Tier 1 (authoritative):** Read each in-scope file's `domain:` frontmatter value. Tier 1 NEVER loses to a lower tier when present.
  - **Tier 2 (optional heuristic):** If `pe-domain-map.yaml` exists at repo root, apply path-slug heuristic for files without declared `domain:`. Map entries are flagged per-file in the Phase 8 report so authors can migrate to declared metadata.
  - **Tier 3 (fallback):** Files unresolved by Tier 1 or Tier 2 receive `domain: unknown`. `unknown` is a reserved domain-id; repos MUST NOT declare `domain: unknown` in any file (the orchestrator emits a Phase 8 warning when a declared value collides with the reserved id).

### Algorithm (deterministic, 5 steps)

1. **Resolve scope to file set.** Expand `--scope` (artifact-type token → all files under root; path → enumerated files; path-set → union). If `--deps direct|full|<N>` is present, traverse the `dependency-tracking` closure to depth N and union into the file set. Tag each file as `role=seed` (named by the caller) or `role=dep` (added by closure traversal).
2. **Resolve domain per file.** For each file, apply Tier 1 → Tier 2 → Tier 3 and record `domain-source` (`declared` | `path-heuristic` | `unknown`). `--dim` is NEVER consulted here — `--dim` is a dimension-group selector per v14 (filters which audit dimensions Phase 2–4 exercises); it never affects domain resolution.
3. **Compute seed footprint and dependency footprint SEPARATELY.** Seed footprint = `{distinct domains across files where role=seed}`. Dependency footprint = `{distinct domains across files where role=dep AND domain ∉ seed footprint}`. Recording the two sets separately is what enables the `cross-domain-deps` discrimination in step 4.
4. **Dispatch from the seed-vs-deps decision matrix.**

   | Seed footprint | Additional dep-domains | Disposition (`bundle=…`) | Phase 1 action |
   |---|---|---|---|
   | 1 | 0 | `single-domain` | Proceed without prompt |
   | 1 | ≥ 1 | `cross-domain-deps` | Proceed as ONE review with per-dep-domain specialized analysis lenses in Phase 2–4 (no split — splitting a single-seed cross-domain-deps invocation produces incomplete reviews because the consumer artifact needs all declared deps present to be evaluated) |
   | ≥ 2 | n/a | `multi-domain-gated` (`--mode apply`) or `multi-domain-advisory` (`--mode plan`) | `apply`: emit numbered split proposal and BLOCK Phase 1 until user selects a split or appends `bundle=accept` (→ `bundle=accepted-bundle`); `plan`: include split proposal in Phase 8 report and proceed |

5. **Emit the first-line `Resolved invocation:` log marker.** Append `| bundle=<disposition>` to the canonical first-line log so the disposition is observable before any side effects occur.

### Outputs

- **Domain footprint table** in the Phase 8 report with one row per in-scope file and columns `path | role (seed/dep) | domain | domain-source (declared/path-heuristic/unknown)`. Files resolved by Tier 2 or Tier 3 are flagged so authors can backfill declared metadata.
- **Numbered split proposal** (only when disposition is `multi-domain-gated` or `multi-domain-advisory`): one numbered line per detected domain plus a final line for "proceed as one atomic bundle (equivalent to appending `bundle=accept`)". Example shape:

   ```text
   Phase 0b detected 3 distinct domains in the resolved scope:

   [1] prompt-engineering  (12 files)
   [2] article-writing     (4 files)
   [3] learning-hub        (2 files)
   [4] proceed as one atomic bundle (bundle=accept)

   Select a split number (1-3) to run that domain only, run all three sequentially (all), or [4] to proceed as one atomic bundle.

   Note: 2 files were resolved by path-heuristic (Tier 2) and 2 files by unknown fallback (Tier 3). Consider adding `domain:` to those files' YAML frontmatter; see the Phase 8 report for the exact file list.
   ```

### Gate behavior summary

| `--mode` | Seed footprint | Additional dep-domains | `bundle=accept` present? | Phase 0b disposition | Phase 1 runs? |
|---|---|---|---|---|---|
| `apply` | 1 | 0 | n/a | `single-domain` | Yes |
| `apply` | 1 | ≥ 1 | n/a | `cross-domain-deps` | Yes (one run; per-dep-domain lenses in Phase 2–4) |
| `apply` | ≥ 2 | n/a | no | `multi-domain-gated` | No — BLOCK on user input |
| `apply` | ≥ 2 | n/a | yes | `accepted-bundle` | Yes |
| `plan` | 1 | 0 | n/a | `single-domain` | Yes |
| `plan` | 1 | ≥ 1 | n/a | `cross-domain-deps` | Yes (one run; per-dep-domain lenses in Phase 2–4) |
| `plan` | ≥ 2 | n/a | n/a | `multi-domain-advisory` | Yes (with advisory in report) |

### `bundle=…` closed set

The first-line `Resolved invocation:` log MUST append `| bundle=<value>` where `<value>` is exactly one of:

- `single-domain`
- `cross-domain-deps`
- `multi-domain-gated`
- `accepted-bundle`
- `multi-domain-advisory`

Any other value is a parser bug and MUST be rejected at adherence-rubric audit (pe-meta-adherence R-Pb).

### `bundle=accept` consent token

Single-keystroke bypass for the multi-domain gate. Appended as a trailing positional token (e.g. `/pe-meta-review --mode apply --scope context bundle=accept`). It is the ONLY accepted consent value — `bundle=skip`, `bundle=yes`, `bundle=true`, etc. are REJECTED with CF-05. Consent is recorded on the first-line `Resolved invocation:` log as `bundle=accepted-bundle` (distinct from the un-consented `bundle=multi-domain-gated` marker so the audit trail preserves the discrimination).

### CF-05 rejection — `--skip domain-coherence`

Phase 0b is NOT in the skippable set. `--skip domain-coherence` is REJECTED with CF-05:

```text
CF-05: --skip domain-coherence is rejected; Phase 0b is not skippable per vision v15 § Domain-coherent batching. To bypass the gate on a multi-domain scope, append bundle=accept to the invocation.
```

### `--dim` orthogonality

`--dim` is a dimension-group selector per v14 (filters which audit dimensions Phase 2–4 exercises). It is NEVER a domain override. The domain footprint is always computed from per-file declared `domain:` metadata (with Tier 2/3 fallback) regardless of the `--dim` value.

### Determinism

The domain map is computed ONCE at Phase 0b entry and frozen for the rest of the run. The 3-tier algorithm runs in < 1 second on the largest current workspace (51 files, 3 domains); each file's YAML frontmatter is read once and cached. No LLM call required.

---

## 📋 Per-prompt-class applicability matrix

| Prompt class | Files (examples) | Phase 0a CF-05 | Phase 0b |
|---|---|---|---|
| Orchestrator-level | `pe-meta-review`, `pe-meta-create-update`, `pe-meta-design`, `pe-meta-adherence`, `pe-meta-scheduled-review`, `pe-meta-release-monitor` | **Skip** (artifact-type-agnostic by design) | **Enforce** on resolved file set |
| Per-artifact | `pe-meta-{context\|prompt\|instruction\|agent\|template\|skill\|hook\|snippet}-{review\|create-update\|design}` | **Enforce** (single pinned root per prompt) | **Enforce** on resolved file set |
| Delegated-only (no direct invocation) | Internal agents (e.g. `pe-con-validator`) | n/a — never invoked directly by users | n/a — runs after Phase 0b on a pre-resolved scope |

**Per-artifact prompt CF-05 root pin.** Each per-artifact prompt locally pins its single expected root in its `## Invocation` (or equivalent) section and cites the canonical mapping table above for the rejection contract. Example: `pe-meta-context-review` pins `.copilot/context/` as its expected root.

---

## 🔗 How to cite this file from a prompt

**Orchestrator-level prompt boilerplate (minimal Phase 0b stub).**

```markdown
## Phase 0b — Domain coherence

This prompt enforces the Phase 0b domain coherence gate defined in
[`04.05-pe-meta-invocation-gates.md`](../../.copilot/context/00.00-prompt-engineering/04.05-pe-meta-invocation-gates.md)
(upstream authority: current vision document § Domain-coherent batching).

**Locally true for this prompt:**
- Scope = `<describe what this prompt's positional/--scope/--deps surface resolves to>`.
- Gate runs BEFORE delegating to `<list of agents/prompts this orchestrator dispatches to>`.
- Consent token `bundle=accept` is recognized; `--skip domain-coherence` is rejected.
- Phase 0a CF-05 **does NOT** apply at this orchestrator-level layer (artifact-type-agnostic).
```

**Per-artifact prompt boilerplate (minimal Phase 0a/0b stub).**

```markdown
## Phase 0a CF-05 + Phase 0b — Invocation gates

This prompt enforces the Phase 0a CF-05 artifact-type/path consistency check
AND the Phase 0b domain coherence gate defined in
[`04.05-pe-meta-invocation-gates.md`](../../.copilot/context/00.00-prompt-engineering/04.05-pe-meta-invocation-gates.md)
(upstream authority: current vision document).

**Locally true for this prompt:**
- CF-05 expected root: `<root>` (from the prompt-name-prefix → expected-root table).
- Scope = positional `<file-path>` (+ closure under `--deps full`).
- Gate runs BEFORE delegating to the orchestrator.
- `bundle=accept` is propagated when delegating so the orchestrator does not re-prompt.
```

**Sibling prompts MUST NOT cite each other** for Phase 0a/0b — they MUST cite this file. The Wave-2 anti-pattern (orchestrators citing another orchestrator's `.prompt.md § Phase 0b` directly) violates peer-to-peer-coupling boundary domain-coherent-batching and is explicitly retired by this file.

---

## 📚 References

- 📗 Vision v15 — [20260531.01-vision.md](../../../06.00-idea/self-updating-prompt-engineering/20260531.01-vision.md) § Domain-coherent batching, § Domain detection, § Pipeline phases and `--skip` mapping — upstream authority for both gates
- 📒 Use case — [p0-04-domain-coherent-batching.md](../../../06.00-idea/self-updating-prompt-engineering/20260503.02-vision-pe-meta-usecases/05-reliability/p0-04-domain-coherent-batching-usecase.md) — originating motivation
- 📘 `pe-meta-review.prompt.md` § Phase 0a precondition + § Phase 0b — primary orchestrator implementation; cites this file as the algorithm SoT
- 📘 `pe-meta-adherence.prompt.md` § Phase 3 rubric extension (R-Pa / R-Pb) — audits CF-05 + `bundle=…` log markers against the closed set defined here
- 📘 `pe-meta-option-parser-tests.md` § Tests P0b-01..P0b-14 — parser-level test cases for the 5-step algorithm
- 📘 [04.04-orchestrator-runtime-validation.md](04.04-orchestrator-runtime-validation.md) — sibling runtime-validation pattern (three-layer gate checks)

---

<!--
context_metadata:
  version: "1.0.1"
  last_updated: "2026-06-25"
  created: "2026-05-31"
  changelog: "04.05-pe-meta-invocation-gates.changelog.md"
-->
