---
title: "pe-meta per-type validation checklists"
description: "Per-type structural validation checklists with exemplary quality bar thresholds — enables self-contained pe-meta validation without delegating to pe-gra validators"
domain: "prompt-engineering"
goal: "Provide internalized type-specific structural validation checklists so pe-meta-validator does not need to delegate to pe-gra validators"
scope:
  covers:
    - "Structural validation checklist per artifact type (8 types)"
    - "Exemplary quality bar thresholds alongside standard bar"
    - "YAML field requirements per type"
    - "Type-specific boundary and reference rules"
  excludes:
    - "Strategic validation criteria (lives in 05.06)"
    - "Dimension execution logic (lives in pe-meta-validator)"
    - "Dimension catalog (lives in 05.07)"
boundaries:
  - "Checklists define WHAT to check, not HOW to check — execution logic is in the validator agent"
  - "Exemplary bar is for PE-for-PE artifacts; standard bar is for domain artifacts"
rationales:
  - "Internalized checklists eliminate pe-gra delegation — single-agent validation is simpler and applies the exemplary bar"
  - "Side-by-side standard/exemplary columns let the validator select the appropriate bar per artifact domain"
---

# pe-meta Per-Type Validation Checklists

## How to use

The pe-meta-validator loads this file and dispatches to the appropriate section based on the target artifact's file path:
- `.copilot/context/` → Context file checklist
- `.agent.md` → Agent checklist
- `.prompt.md` → Prompt checklist
- `.instructions.md` → Instruction file checklist
- `SKILL.md` → Skill checklist
- `.template.md` → Template checklist
- `.json` in `hooks/` → Hook checklist
- `prompt-snippets/` → Snippet checklist

> **`Deterministic?` column + sub-check granularity (per `dimension-rule-self-containment`, vision v15.5).** In every table below, each row is one **sub-check**, and the `Deterministic?` column records whether that sub-check is mechanically computable (✅ Yes), needs reasoning (❌ No), or is a composite/partial judgment (⚠️ Partial). A dimension's verdict consolidates *all* its rows — it is `verified` only when every applicable row is evaluated and passes; an unevaluated row makes the dimension `partial` (see [evidence-coverage contract](../../../.github/prompt-snippets/pe-meta-evidence-coverage.md) § graded verdict). A composite dimension (e.g. `D5`, `D14`) splits across deterministic and reasoning rows, so its dimension-level `⚠️ Partial` in [05.07](05.07-pe-meta-dimension-catalog.md) is decomposed here.

> **Metadata field placement (per [00.03-metadata-contracts](00.03-metadata-contracts.md)).** Where a checklist row references `version:` or `last_updated:`, evaluate those fields against the artifact's **bottom `{type}_metadata` HTML-comment block** — NOT the top frontmatter. PE artifacts carry change-prone tracking fields (`version`, `last_updated`, `created`, optional `changelog:`) only in the bottom block; finding them in top frontmatter is itself a defect. **Exception:** vision documents (`06.00-idea/**/*vision*.md`) and use-case documents (`**/*usecases/**`) keep `version`/`last_updated` in top frontmatter because their amendment protocols read it there.

---

## Context files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Deterministic? | Dimension |
|---|---|---|---|---|
| `title:` present | Required | Required | ✅ Yes | D1-metadata |
| `description:` present | Required | Required | ✅ Yes | D1-metadata |
| `version:` SemVer | Required | Required | ✅ Yes | D1-metadata |
| `last_updated:` date | Required | Required | ✅ Yes | D1-metadata |
| `goal:` single sentence | Recommended | Required | ✅ Yes | D1-metadata |
| `scope.covers:` list | Recommended | Required | ✅ Yes | D1-metadata |
| `scope.excludes:` list | Optional | Required | ✅ Yes | D1-metadata |
| `boundaries:` list | Optional | Required (≥2) | ✅ Yes | D1-metadata |
| `rationales:` list | Optional | Required (≥1) | ✅ Yes | D1-metadata |
| All `📖` refs resolve | Required | Required | ✅ Yes | D2-references |
| All markdown links resolve | Required | Required | ✅ Yes | D2-references |
| Token count ≤2,500 (hard ceiling; prefer ≤1,000 when practical) | Required | Required | ✅ Yes | D3-token-budget |
| N-1 separation on rule sections | Recommended | Required | ⚠️ Partial | D14-craftsmanship |
| Naming: kebab-case with tier prefix | Required | Required | ✅ Yes | D14-craftsmanship |
| **No re-hosted rule** — no section restates a rule canonical in another file; the context file points, it does not duplicate (authority: single-source-of-truth) | Recommended | Required | ⚠️ Partial | D14-craftsmanship / D23-reference-efficiency |

---

## Agent files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Deterministic? | Dimension |
|---|---|---|---|---|
| `description:` present | Required | Required | ✅ Yes | D1-metadata |
| `agent:` mode (plan/agent) | Required | Required | ✅ Yes | D1-metadata |
| `tools:` list | Required | Required | ✅ Yes | D1-metadata |
| `version:` SemVer | Recommended | Required | ✅ Yes | D1-metadata |
| `goal:` single sentence | Recommended | Required | ✅ Yes | D1-metadata |
| `scope.covers:` + `excludes:` | Recommended | Required | ✅ Yes | D1-metadata |
| `boundaries:` list | Recommended | Required | ✅ Yes | D1-metadata |
| `rationales:` list | Optional | Required (≥2) | ✅ Yes | D1-metadata |
| `handoffs:` with `send: true` where needed | Recommended | Required | ✅ Yes | D1-metadata |
| `context_dependencies:` declared | Recommended | Required | ✅ Yes | D1-metadata |
| All `📖` refs resolve | Required | Required | ✅ Yes | D2-references |
| All markdown links resolve | Required | Required | ✅ Yes | D2-references |
| All slash-command refs resolve | Required | Required | ✅ Yes | D2-references |
| Handoff targets resolve to existing agents | Required | Required | ✅ Yes | D2-references |
| Token count ≤2,500 | Required | Required | ✅ Yes | D3-token-budget |
| `plan` mode → read-only tools only | Required | Required | ✅ Yes | D4-tool-alignment |
| `agent` mode → includes write tools | Required | Required | ✅ Yes | D4-tool-alignment |
| Tool count 3-7 | Required | Required | ✅ Yes | D4-tool-alignment |
| Boundaries: ≥3 Always / ≥1 Ask / ≥2 Never | Standard | Exemplary: ≥5/≥2/≥3 | ✅ Yes | D5-boundaries |
| **H14 collective grounding** — body carries a single directive that enforces all YAML `boundaries:` with precedence over body content; metadata wins on conflict (authority: [00.03-metadata-contracts.md](00.03-metadata-contracts.md) § Grounding alignment rule) | Required | Required | ✅ Yes | D5-boundaries |
| **No verbatim restatement** — no body entry copies a YAML boundary verbatim; per-boundary body twins are not permitted (authority: [00.03-metadata-contracts.md](00.03-metadata-contracts.md) § Grounding alignment rule) | Required | Required | ✅ Yes | D5-boundaries |
| **No contradiction/weakening** — no body entry overrides, weakens, or deprioritizes a YAML boundary (authority: [00.03-metadata-contracts.md](00.03-metadata-contracts.md) § Grounding alignment rule) | Required | Required | ✅ Yes | D5-boundaries |
| **Operationalize-not-restate** — body boundary entries are additive: they operationalize metadata (thresholds/escalation triggers/sequencing), not restate it (authority: [02.04-agent-shared-patterns.md](02.04-agent-shared-patterns.md) runtime-grounding) | Required | Required | ✅ Yes | D5-boundaries |
| **Claim/implementation match** — `goal:`/`description:` claims (e.g. enumerated mode/capability counts) match the body implementation | Required | Required | ❌ No | D6-consistency |
| Persona section present | Required | Required | ✅ Yes | D14-craftsmanship |
| Process phases defined | Required | Required | ✅ Yes | D14-craftsmanship |
| Test scenarios (≥3) | Required | Required | ✅ Yes | D14-craftsmanship |
| **No re-hosted contract** — no body section re-hosts content canonical in a referenced snippet; the agent points, it does not restate (authority: reference-based architecture) | Recommended | Required | ⚠️ Partial | D14-craftsmanship / D23-reference-efficiency |
| **Section proportionality + early-commands** — critical boundaries sit within the first ~30% of the body [M2]; no contract/reference section dwarfs the core Process | Recommended | Required | ⚠️ Partial | D19-artifact-structure |
| Bottom metadata key = `agent_metadata:` (canonical per in-scope majority; `article_metadata:` is the article convention) | Required | Required | ✅ Yes | D30-metadata-guard |

---

## Prompt files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Deterministic? | Dimension |
|---|---|---|---|---|
| `name:` slash-command | Required | Required | ✅ Yes | D1-metadata |
| `description:` present | Required | Required | ✅ Yes | D1-metadata |
| `agent:` mode (plan/agent) | Required | Required | ✅ Yes | D1-metadata |
| `model:` specified | Recommended | Required | ✅ Yes | D1-metadata |
| `tools:` list | Required | Required | ✅ Yes | D1-metadata |
| `version:` SemVer | Recommended | Required | ✅ Yes | D1-metadata |
| `goal:` single sentence | Recommended | Required | ✅ Yes | D1-metadata |
| `scope.covers:` + `excludes:` | Recommended | Required | ✅ Yes | D1-metadata |
| `argument-hint:` usage example | Recommended | Required | ✅ Yes | D1-metadata |
| `rationales:` list | Optional | Required (≥1) | ✅ Yes | D1-metadata |
| All `📖` refs resolve | Required | Required | ✅ Yes | D2-references |
| All markdown links resolve | Required | Required | ✅ Yes | D2-references |
| All slash-command refs resolve | Required | Required | ✅ Yes | D2-references |
| Agent refs in `handoffs:` resolve | Required | Required | ✅ Yes | D2-references |
| Token count ≤3,000 | Required | Required | ✅ Yes | D3-token-budget |
| `plan` mode → read-only tools only | Required | Required | ✅ Yes | D4-tool-alignment |
| `agent` mode → includes write tools | Required | Required | ✅ Yes | D4-tool-alignment |
| Tool count 3-7 | Required | Required | ✅ Yes | D4-tool-alignment |
| **Scope-enforcement step present** — the workflow includes a step that verifies the request falls within declared scope before proceeding (prompts ground boundaries as a workflow step, not a three-tier section; authority: [00.03-metadata-contracts.md](00.03-metadata-contracts.md) § Grounding alignment rule) | Recommended | Required | ⚠️ Partial | D5-boundaries |
| **Claim/implementation match** — `goal:`/`description:` claims (e.g. enumerated phase/capability counts) match the body workflow | Required | Required | ❌ No | D6-consistency |
| **Test scenarios (≥3)** — binds **orchestrator-class** prompts only (user-invoked entry points carrying `--option` flags, e.g. `pe-meta-review`, `pe-meta-create-update`, `pe-meta-adherence`, `pe-meta-scheduled-review`). **Per-type sub-prompts** (e.g. `pe-meta-agent-design`, `pe-meta-prompt-review`) are exempt — they rely on the invoking orchestrator's scenario suite plus their own `argument-hint` example | Required (orchestrator-class) / N/A (per-type) | Required (orchestrator-class) / N/A (per-type) | ✅ Yes | D14-craftsmanship |
| **No re-hosted contract** — no body section re-hosts content canonical in a referenced snippet; the prompt points, it does not restate (authority: reference-based architecture) | Recommended | Required | ⚠️ Partial | D14-craftsmanship / D23-reference-efficiency |
| **Section proportionality + early-commands** — critical constraints sit within the first ~30% of the body [M2]; no contract/reference section dwarfs the core workflow | Recommended | Required | ⚠️ Partial | D19-artifact-structure |

---

## Instruction files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Deterministic? | Dimension |
|---|---|---|---|---|
| `description:` present | Required | Required | ✅ Yes | D1-metadata |
| `applyTo:` glob pattern | Required | Required | ✅ Yes | D1-metadata |
| `version:` SemVer | Recommended | Required | ✅ Yes | D1-metadata |
| `goal:` single sentence | Recommended | Required | ✅ Yes | D1-metadata |
| `rationales:` list | Optional | Required (≥1) | ✅ Yes | D1-metadata |
| All `📖` refs resolve | Required | Required | ✅ Yes | D2-references |
| Token count ≤1,500 | Required | Required | ✅ Yes | D3-token-budget |
| Rules are testable/mechanical only (instruction-minimization) | Required | Required | ⚠️ Partial | D14-craftsmanship |
| No behavioral/strategic rules (instruction-minimization) | Required | Required | ⚠️ Partial | D14-craftsmanship |
| Severity index present *(see scope note)* | Recommended | Required | ✅ Yes | D14-craftsmanship |

> **Severity-index scope note.** The severity index is a *validator-facing projection of the global `validation-rules` matrix* onto the artifact type an instruction file governs (its entries reference global codes `[C#]`/`[H#]`/`[M#]`). It therefore applies **only to instruction files that govern PE artifacts validated by that matrix** (prompts, agents, context files, skills, hooks, prompt-snippets, templates, instruction files). For **document-governance instruction files** — those whose `applyTo` targets user-content documents that no PE validator runs the matrix against (e.g. `use-case-documents`, `vision-amendment`, `vision-frontmatter`) — the severity index is **N/A**: their rules are single-file-class document-shape rules with no representation in the global matrix, and adding one would either project 2–3 barely-applicable codes or manufacture new severity codes. Treat such files as **standard bar** and do not flag a missing severity index.

---

## Skill files (SKILL.md)

| Check | Standard bar | Exemplary bar (PE-for-PE) | Deterministic? | Dimension |
|---|---|---|---|---|
| `name:` present | Required | Required | ✅ Yes | D1-metadata |
| `description:` present (trigger phrases) | Required | Required | ✅ Yes | D1-metadata |
| All `📖` refs resolve | Required | Required | ✅ Yes | D2-references |
| Token count ≤2,500 | Required | Required | ✅ Yes | D3-token-budget |
| Progressive disclosure (3 levels) | Recommended | Required | ⚠️ Partial | D14-craftsmanship |

---

## Template files

| Check | Standard bar | Exemplary bar (PE-for-PE) | Deterministic? | Dimension |
|---|---|---|---|---|
| Filename ends with `.template.md` | Required | Required | ✅ Yes | D1-metadata |
| Placeholders documented | Recommended | Required | ⚠️ Partial | D1-metadata |
| All `📖` refs resolve | Required | Required | ✅ Yes | D2-references |
| Token count ≤2,500 | Required | Required | ✅ Yes | D3-token-budget |
| Schema sections present | Required | Required | ✅ Yes | D14-craftsmanship |

---

## Hook files (JSON)

| Check | Standard bar | Exemplary bar (PE-for-PE) | Deterministic? | Dimension |
|---|---|---|---|---|
| Valid JSON | Required | Required | ✅ Yes | D1-metadata |
| `event:` field present | Required | Required | ✅ Yes | D1-metadata |
| `steps:` array present | Required | Required | ✅ Yes | D1-metadata |
| All file paths in steps resolve | Required | Required | ✅ Yes | D2-references |

---

## Prompt snippets

| Check | Standard bar | Exemplary bar (PE-for-PE) | Deterministic? | Dimension |
|---|---|---|---|---|
| Filename follows naming convention | Required | Required | ✅ Yes | D1-metadata |
| All `📖` refs resolve | Required | Required | ✅ Yes | D2-references |
| Token count ≤1,000 | Required | Required | ✅ Yes | D3-token-budget |
| Reusable (no hardcoded paths) | Recommended | Required | ⚠️ Partial | D14-craftsmanship |

<!--
context_metadata:
  filename: "05.08-pe-meta-type-checklists.md"
  created: "2026-05-15"
  last_updated: "2026-06-25"
  version: "1.5.0"
  type: "context"
  changelog: "05.08-pe-meta-type-checklists.changelog.md"
-->
