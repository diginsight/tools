---
title: "Tool composition guide"
description: "Tool selection, composition patterns, priority hierarchy, L1/L2 architecture, and mode alignment rules for prompts and agents"
domain: "prompt-engineering"
goal: "Define safe and efficient tool selection and composition rules for prompts and agents"
scope:
   covers:
      - "Tool priority hierarchy and composition patterns"
      - "Mode alignment rules for read-only versus read-write workflows"
      - "Selection guidance based on risk, cost, and capability"
   excludes:
      - "Prompt content quality heuristics unrelated to tools"
      - "Provider-specific model tuning guidance"
boundaries:
   - "MUST enforce least-privilege tool selection by default"
   - "MUST keep plan versus agent mode alignment explicit"
   - "MUST NOT duplicate token budget tables from 01.06-system-parameters.md"
rationales:
   - "Well-composed tool sets reduce failure modes and execution cost"
   - "Stable composition rules improve validator determinism and safety"
authoritative_sources:
  - url: "https://code.visualstudio.com/docs/agent-customization/custom-agents"
    description: "VS Code custom agents — tools property, allowedTools, tool restrictions"
---

# Tool Composition Guide for GitHub Copilot

**Purpose**: Tool selection rules, architecture, and composition patterns for prompts and agents.

**Referenced by**: `.github/instructions/pe-prompts.instructions.md`, `.github/instructions/pe-agents.instructions.md`, all builder and researcher agents

📖 **Detailed examples and recipes:** `.github/templates/00.00-prompt-engineering/reference-tool-catalog.template.md`

---

## Tool Priority Rules

### Priority Hierarchy

When GitHub Copilot has access to tools from multiple sources, it applies them in this order:

1. **Prompt-level tools** (highest priority)
   - Defined in `tools:` YAML frontmatter of `.prompt.md` files
   - Invoked via `/promptName` command
   - Scope: Only while that specific prompt is executing

2. **Agent-level tools**
   - Defined in `tools:` YAML frontmatter of `.agent.md` files
   - Active when that agent is selected in agent picker
   - Scope: Entire conversation while agent is active

3. **Default tools** (lowest priority)
   - Built-in VS Code Copilot tools
   - Always available regardless of prompt/agent
   - Scope: All conversations

**Example**:
```yaml
# .github/prompts/example.prompt.md
---
tools: ['read_file']  # ONLY read_file available during prompt execution
---

# .github/agents/example.agent.md  
---
tools: ['read_file', 'semantic_search', 'create_file']  # 3 tools when agent active
---

# Default tools when no prompt/agent active:
# read_file, semantic_search, create_file, replace_string_in_file, grep_search, etc.
```

### When to Define Tools at Each Level

| Level | Use When | Example |
|-------|----------|---------|
| **Prompt-level** | Task requires tool restriction beyond agent's normal scope | Grammar validator prompt needs `agent: plan` + read-only tools, but agent normally has write access |
| **Agent-level** | Role consistently needs specific toolset | Researcher agent always needs `semantic_search`, `grep_search`, `read_file` but never write tools |
| **Default** | General-purpose tasks with no special restrictions | Quick questions, exploratory code reviews |

---

## Tool Architecture Levels

Tools operate in two distinct layers:

### Level 1: Always-Available Tools

These tools are **always accessible** regardless of prompt/agent configuration. Never list them in `tools:` frontmatter — they're implicit.

| Tool | Purpose | Token Cost |
|------|---------|------------|
| `manage_todo_list` | Track task progress | ~200 |
| `ask_questions` | Clarify user intent | ~300 |
| `runSubagent` | Delegate to child agents | 5,000-50,000 |
| `tool_search_tool_regex` | Discover deferred tools | ~100 |

### Level 2: Configurable Tools

These require explicit `tools:` declaration. Split into three categories:

| Category | Tools | Typical Context Cost |
|----------|-------|---------------------|
| **Read** | `read_file`, `grep_search`, `semantic_search`, `file_search`, `list_dir`, `get_errors` | 100-2,000 per call |
| **Write** | `create_file`, `replace_string_in_file`, `multi_replace_string_in_file` | 200-1,000 per call |
| **Execute** | `run_in_terminal`, `fetch_webpage` | 500-10,000 per call |

### Tool Sets (Shorthand)

Use tool set aliases instead of listing individual tools:

| Set | Expands To | Use For |
|-----|-----------|---------|
| `#edit` | All read + write tools | Builders, updaters |
| `#search` | `semantic_search`, `grep_search`, `file_search` | Researchers |
| `#reader` | `read_file`, `list_dir`, `get_errors` | Validators |

```yaml
# Instead of listing 6 individual tools:
tools: ['#edit', 'run_in_terminal']

# For read-only research:
tools: ['#search', '#reader']
```

### Minimal Necessary Tool Surface

Declaring a tool injects its schema into context (~100–500 tokens per tool) on **every** agent step, independent of whether the tool is ever called. Declare only the minimal set an agent or prompt needs:

- Prefer tool-set aliases over long explicit lists.
- Drop tools that overlap or are never exercised by the workflow.
- Audit the active MCP and extension surface periodically; disable unused servers and use a focused profile.

This is the efficiency counterpart of the `D4-tool-alignment` review dimension ("each declared tool is necessary; no over-permissioning"). 📖 Per-interaction context economics: [02.02-context-window-and-token-optimization.md](./02.02-context-window-and-token-optimization.md) → "Tool-surface tax and audit habit".

---


## Tool Categories

### Read-Only Tools (Safe for `agent: plan`)

| Tool | Best For | Cost |
|------|----------|------|
| `read_file` | Reading specific known files | Low |
| `semantic_search` | Finding relevant code by meaning | Medium |
| `grep_search` | Finding exact strings/patterns | Low |
| `file_search` | Finding files by name/glob | Very Low |
| `list_dir` | Listing directory contents | Very Low |
| `get_errors` | Checking compile/lint errors | Low |

### Write Tools (Requires `agent: agent`)

| Tool | Best For | Risk |
|------|----------|------|
| `create_file` | Creating new files | Medium |
| `replace_string_in_file` | Targeted single edit | Low |
| `multi_replace_string_in_file` | Batch editing across files | Medium |

### External Tools (Use with caution)

| Tool | Best For | Risk |
|------|----------|------|
| `fetch_webpage` | Retrieving documentation (public URLs only) | Medium |
| `run_in_terminal` | Executing shell commands | Critical |
| `runSubagent` | Complex multi-step research | Medium |

📖 **Composition patterns and workflows:** `.github/templates/00.00-prompt-engineering/reference-tool-catalog.template.md`

---

## Tool Selection by Agent Role

| Role | Agent Mode | Required Tools | Never Include |
|------|-----------|----------------|---------------|
| **Researcher** | `plan` | `semantic_search`, `grep_search`, `read_file`, `file_search`, `fetch_webpage` | Write tools, `run_in_terminal` |
| **Builder** | `agent` | `read_file`, `semantic_search`, `create_file`, `file_search` | `replace_string_in_file`, `run_in_terminal` |
| **Validator** | `plan` | `read_file`, `grep_search`, `get_errors` | Write tools, `semantic_search` (costly) |
| **Updater** | `agent` | `read_file`, `grep_search`, `replace_string_in_file`, `multi_replace_string_in_file` | `create_file` |
| **Orchestrator** | varies | `read_file`, `semantic_search` + handoffs | Write tools (delegates, doesn't implement) |

📖 **Full role descriptions with workflows:** `.github/templates/00.00-prompt-engineering/reference-tool-catalog.template.md`

---

## Tool Composition Anti-Patterns

| Anti-Pattern | Problem | Fix |
|-------------|---------|-----|
| **Over-Scoping** | Including tools "just in case" (researcher with write tools) | Only include tools essential for role |
| **Under-Scoping** | Missing critical tools (builder without `read_file`) | Include tools for complete workflow |
| **Type/Tool Conflict** | `agent: plan` with write tools | Align agent type with capabilities |
| **Redundant Definitions** | Same tools at prompt and agent level | Only define prompt-level if restricting |
| **Unbounded External** | `run_in_terminal` without constraints | Define explicit command boundaries |

📖 **Detailed examples with code:** `.github/templates/00.00-prompt-engineering/reference-tool-catalog.template.md`

---
## Security and Risk Management

| Tool Category | Risk Level | Mitigation |
|---------------|------------|------------|
| Read-only | ✅ Low | Use freely for research |
| Single write | ⚠️ Medium | Validate target path, use three-tier boundaries |
| Batch write | ⚠️ High | Require explicit user approval |
| Terminal | 🚫 Critical | Define explicit command whitelist |
| External fetch | ⚠️ Medium | Only fetch from trusted domains |

---

## Performance Considerations

### Tool Result Impact on Context Window

Tool results accumulate in conversation and grow the context window:

| Tool Call | Typical Result Size |
|---|---|
| `read_file` (50 lines) | ~500 tokens |
| `read_file` (500 lines) | ~5,000 tokens |
| `grep_search` (10 matches) | ~800 tokens |
| `semantic_search` (10 results) | ~2,000 tokens |
| `fetch_webpage` | ~3,000–10,000 tokens |
| `file_search` (20 results) | ~300 tokens |

**Rule**: Tool results are the **primary target** for token optimization (20–60% of typical context). Use narrow `read_file` ranges, limit search results, prefer `file_search` over `semantic_search` when filename pattern is known.

**📖 Optimization strategies**: [02.02-context-window-and-token-optimization.md](./02.02-context-window-and-token-optimization.md)

### MCP Tool Integration

MCP tools appear alongside built-in L2 tools. Use `tools: ['mcp: server-name']` to scope. Discovery via `tool_search_tool_regex` (deferred loading).

**📖 MCP design patterns**: [03.04-mcp-server-design-patterns.md](./03.04-mcp-server-design-patterns.md)

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 1.0.0 | 2025-12-10 | Initial consolidated version from analysis | System |
| 1.1.0 | 2026-02-22 | Added L1/L2 architecture, tool sets, cross-references | System |
| 1.2.0 | 2026-02-23 | Added tool result impact table, MCP tool integration section | System |
| 2.1.0 | 2026-03-19 | Compressed: removed verbose YAML examples, optimization patterns (→02.02), tool cost table (kept result impact only) | System |
| 2.3.0 | 2026-06-25 | Added "Minimal Necessary Tool Surface" subsection (schema tax ~100–500 tokens/tool/step, audit habit, D4-tool-alignment counterpart) | System |

---

## References

- **Internal**: [01.01-context-engineering-principles.md](./01.01-context-engineering-principles.md), [02.02-context-window-and-token-optimization.md](./02.02-context-window-and-token-optimization.md), [03.04-mcp-server-design-patterns.md](./03.04-mcp-server-design-patterns.md)
- **Sources** (practical examples):
  - [09.50-how_to_leverage_tools_in_prompt_orchestrations.md](../../../03.00-tech/05.02-prompt-engineering/04-howto/09.50-how-to-leverage-tools-in-prompt-orchestrations.md) — search tool decision tree, common capability combinations, practical patterns (context gathering, editing, analysis), per-tool token cost analysis, tool restrictions and safety
- **External**: [VS Code Copilot Customization](https://code.visualstudio.com/docs/agent-customization/overview)

<!--
context_metadata:
  version: "2.3.0"
  last_updated: "2026-06-25"
-->
