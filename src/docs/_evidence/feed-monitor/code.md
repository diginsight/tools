---
title: "feed-monitor — code evidence"
publish: false
component: "feed-monitor"
area: "code"
---

# feed-monitor — code

<!-- Generated by /01.01-ad-docs-investigate. Do not edit by hand — re-run investigation instead. -->

**Component**: `feed-monitor` · `src/30.00 Job/Diginsight.Tools.FeedMonitor/`
**Repository revision observed**: `016193f` (branch `main`)
**Investigated**: 2026-08-16 · **re-investigated**: 2026-08-16 (v1.1.0)

Every locator below is `file:line` relative to the component root unless an absolute repository path is given. Line numbers are anchored to the revision above; re-anchor by symbol name if they drift.

---

## 🧾 Records

### Composition root

| id | assertion | where | kind | environment | observed | confidence |
|---|---|---|---|---|---|---|
| `code-01` | The host is built with `Host.CreateApplicationBuilder()` — a generic host, not a web host | `Program.cs:30` | source | n/a | 2026-08-16 | established |
| `code-02` | `FeedMonitorBackgroundService` is the single hosted service | `Program.cs:84` | source | n/a | 2026-08-16 | established |
| `code-03` | `TimeProvider.System` is registered as a singleton, so time is injectable and therefore substitutable | `Program.cs:79` | source | n/a | 2026-08-16 | established |
| `code-04` | Observability is wired through `AddObservability()`; the component emits through Diginsight rather than raw `ILogger` calls alone | `Program.cs:38` | source | n/a | 2026-08-16 | established |
| `code-05` | A default `HttpClient` is registered with a `BodyLoggingHandler` message handler | `Program.cs:42-43` | source | n/a | 2026-08-16 | established |
| `code-06` | `IParallelService` is registered and provides the bounded-concurrency primitive the run loop uses | `Program.cs:40` | source | n/a | 2026-08-16 | established |
| `code-07` | Six configuration objects are bound as **named** options instances, not as a single tree | `Program.cs:45-77` | configuration | n/a | 2026-08-16 | established |

Named options instances established at `Program.cs:45-77`:

| Section | Options type | Instance name | Binding |
|---|---|---|---|
| `FeedMonitor` | `FeedMonitorConfiguration` | *(default)* | class-aware + dynamic + volatile |
| `FeedMonitor:CosmosDB:*` | `CosmosDBClientConfiguration` | `FeedMonitorCosmosDBOptions` | composed key by key |
| `FeedMonitor:BlobStorage` | `BlobClientConfiguration` | `FeedMonitorBlobStorageOptions` | whole section |
| `FeedMonitor:TableStorage` | `TableClientConfiguration` | `FeedMonitorTableStorageOptions` | whole section |
| `FeedMonitor:FileStorage:ConnectionString` | `FileStorageClientConfiguration` | `FeedMonitorFileStorageOptions` | single key |
| `FeedMonitor:QueueStorage:ConnectionString` | `QueueStorageClientConfiguration` | `FeedMonitorQueueStorageOptions` | single key |

### Entry point and run shape

| id | assertion | where | kind | environment | observed | confidence |
|---|---|---|---|---|---|---|
| `code-08` | Execution begins at `ExecuteAsync(CancellationToken)` | `FeedMonitorBackgroundService.cs:107` | source | n/a | 2026-08-16 | established |
| `code-09` | The run is a single pass: `ReadAllFeedsAsync` is composed into a work list and awaited through `IParallelService.WhenAllAsync()` | `FeedMonitorBackgroundService.cs:112`, `:116` | source | n/a | 2026-08-16 | established |
| `code-10` | The process terminates itself by calling `applicationLifetime.StopApplication()` after the pass — it is a **job**, not a long-lived daemon | `FeedMonitorBackgroundService.cs:124` | source | n/a | 2026-08-16 | established |
| `code-11` | `OperationCanceledException` is caught and treated as graceful shutdown with no further action | `FeedMonitorBackgroundService.cs:120` | source | n/a | 2026-08-16 | established |
| `code-12` | Nothing in the component schedules the run; scheduling is external to it | absence established across `Program.cs` and `FeedMonitorBackgroundService.cs` | source | n/a | 2026-08-16 | established |
| `code-42` | The feed content is fetched at `httpClient.GetStringAsync(feedUri)`, inside the per-feed lambda passed to `parallelService.ForEachAsync` | `FeedMonitorBackgroundService.cs:165`, lambda at `:150`, enclosing method `ReadAllFeedsAsync` at `:133` | source | n/a | 2026-08-16 | established |
| `code-43` | The parser factory is invoked at `feedParserFactory.ParseFeed(xmlContent)`, in the same lambda; the factory is constructor-injected | `FeedMonitorBackgroundService.cs:178`, injected at `:66` | source | n/a | 2026-08-16 | established |
| `code-44` | **The registered `HttpClient` is not the one used.** The fetch constructs `new HttpClient()` directly, bypassing the factory registration and its `BodyLoggingHandler` | `FeedMonitorBackgroundService.cs:162` versus `Program.cs:42-43` | source | n/a | 2026-08-16 | established |
| `code-45` | **No inbound reference exists.** No `.csproj` outside the component declares a `ProjectReference` to `Diginsight.Tools.FeedMonitor.csproj` | absence established by searching every `.csproj` under `src/` | source | n/a | 2026-08-16 | established |
| `code-46` | Each sink write is guarded by its own `Enabled` flag: Cosmos at `:212`, Table at `:220`, Blob at `:224` | `FeedMonitorBackgroundService.cs:212`, `:220`, `:224` | source | n/a | 2026-08-16 | established |

### Feed parsing

| id | assertion | where | kind | environment | observed | confidence |
|---|---|---|---|---|---|---|
| `code-13` | Parsing is behind `IFeedParserFactory` | `Abstractions/IFeedParserFactory.cs` | source | n/a | 2026-08-16 | established |
| `code-14` | Two parser implementations exist — `RSSFeedParser` and `AtomFeedParser` — both deriving from `FeedParserBase` | `Helper/RSSFeedParser.cs`, `Helper/AtomFeedParser.cs`, `Helper/FeedParserBase.cs` | source | n/a | 2026-08-16 | established |
| `code-15` | Implementation selection is **content-based, not configuration-based**: `GetParser(XDocument)` asks `rssFeedParser.CanParse` first, then `atomFeedParser.CanParse` | `Helper/FeedParserFactory.cs:71-75` | source | n/a | 2026-08-16 | established |
| `code-16` | Order matters: RSS is tried first, so a document both parsers claim resolves to RSS | `Helper/FeedParserFactory.cs:71-75` | source | n/a | 2026-08-16 | established |
| `code-17` | Both parsers and the factory are registered as singletons through `AddFeedParsing()` | `Extensions/FeedParsingServiceCollectionExtensions.cs` | source | n/a | 2026-08-16 | established |
| `code-37` | `RSSFeedParser.CanParse` matches on root element `rss` only | `Helper/RSSFeedParser.cs:20-22` | source | n/a | 2026-08-16 | established |
| `code-38` | `AtomFeedParser.CanParse` matches on root element `feed` only | `Helper/AtomFeedParser.cs:14-16` | source | n/a | 2026-08-16 | established |
| `code-39` | The two predicates are **mutually exclusive** — a root element cannot be both — so `code-16`'s ordering never actually decides anything | `Helper/RSSFeedParser.cs:20-22` + `Helper/AtomFeedParser.cs:14-16` | source | n/a | 2026-08-16 | established |
| `code-40` | A document claimed by **neither** parser causes `GetParser(XDocument)` to throw `InvalidOperationException` | `Helper/FeedParserFactory.cs:100` | source | n/a | 2026-08-16 | established |
| `code-41` | `GetParser(XDocument)` returns a `FeedParserBase`; `ParseFeed(string)` returns a `FeedChannelBase` | `Helper/FeedParserFactory.cs:84`, `:38` | source | n/a | 2026-08-16 | established |

### Domain model

| id | assertion | where | kind | environment | observed | confidence |
|---|---|---|---|---|---|---|
| `code-18` | The model is a two-level hierarchy — `FeedChannelBase` and `FeedItemBase`, both over `EntityBase` — with RSS and Atom concretions | `Models/Base/` | source | n/a | 2026-08-16 | established |
| `code-19` | `EntityBase` carries the audit fields `Id`, `Type`, `DateCreated`, `DateModified`, `Status`, `CreatedBy`, `ModifiedBy` | `Models/Base/EntityBase.cs` | source | n/a | 2026-08-16 | established |
| `code-20` | Polymorphism across the two hierarchies is handled by dedicated JSON converters at the persistence boundary | `Helper/FeedItemBaseConverter.cs`, `Helper/FeedChannelBaseConverter.cs` | source | n/a | 2026-08-16 | established |
| `code-21` | `FeedItemBase.PartitionKey` is a model property, so the persistence key is carried on the domain object rather than computed at the sink | `Models/Base/FeedItemBase.cs` | source | n/a | 2026-08-16 | established |

### Persistence surfaces

| id | assertion | where | kind | environment | observed | confidence |
|---|---|---|---|---|---|---|
| `code-22` | Cosmos DB — channel upsert | `FeedMonitorBackgroundService.cs:245`, called from `:212` | source | n/a | 2026-08-16 | established |
| `code-23` | Cosmos DB — item upsert, partitioned by a year-derived key | `FeedMonitorBackgroundService.cs:291`, called from `:214` | source | n/a | 2026-08-16 | established |
| `code-24` | Table Storage — item write, `PartitionKey` year-derived and `RowKey` from the item `Guid` | `FeedMonitorBackgroundService.cs:427`, called from `:217` | source | n/a | 2026-08-16 | established |
| `code-25` | Blob Storage — item write, one folder per item holding `metadata.json`, `content.md` and `description.md` | `FeedMonitorBackgroundService.cs:553`, called from `:220`; upload at `:677` | source | n/a | 2026-08-16 | established |
| `code-26` | **All three sinks are on the main run path.** The Table sink is reachable and live | call sites `:212`, `:214`, `:217`, `:220` | source | n/a | 2026-08-16 | established |
| `code-28` | Cosmos client construction supports three authentication shapes: a connection string, an endpoint plus key, and a token credential | `FeedMonitorBackgroundService.cs:980`, `:984`, `:989-992` | source | n/a | 2026-08-16 | established |
| `code-27` | File Storage and Queue Storage are **configured but unreachable** — options are bound at `Program.cs:69-77` and no code path consumes them | `Program.cs:69-77` + absence across the component | source | n/a | 2026-08-16 | established |
| `code-47` | The four sink-call line numbers recorded in `code-26` (`:212`, `:214`, `:217`, `:220`) and the three guard lines in `code-46` (`:212`, `:220`, `:224`) were read in two separate passes and do not fully agree. `:212` is common to both; the others differ | two independent readings of `FeedMonitorBackgroundService.cs` | source | n/a | 2026-08-16 | claimed |

### Telemetry payload sites

| id | assertion | where | kind | environment | observed | confidence |
|---|---|---|---|---|---|---|
| `code-29` | Six activity payloads receive a **whole client-configuration object** rather than selected scalar fields | `FeedMonitorBackgroundService.cs:743`, `:761`, `:873`, `:969`, `:1006`, `:1049` | source | n/a | 2026-08-16 | established |
| `code-30` | The configuration types reaching those payloads declare properties named `ConnectionString`, `AuthKey`, `SasToken` and `AccountKey` | `Configuration/CosmosDBClientConfiguration.cs`, `Configuration/BlobClientConfiguration.cs`, `Configuration/TableClientConfiguration.cs` | source | n/a | 2026-08-16 | established |
| `code-31` | `FeedMonitorConfiguration` declares `TenantId`, `ClientId` and `ClientSecret` | `Configuration/FeedMonitorConfiguration.cs` | source | n/a | 2026-08-16 | established |
| `code-32` | No redaction attribute is applied to any of those properties at this revision | absence established by reading every file under `Configuration/` | source | n/a | 2026-08-16 | established |
| `code-33` | Four further payload sites receive a domain object (`FeedChannelBase`), not a configuration object | `FeedMonitorBackgroundService.cs:245`, `:291`, `:427`, `:553` | source | n/a | 2026-08-16 | established |
| `code-34` | `:677` places the full article `content` string into the upload activity payload | `FeedMonitorBackgroundService.cs:677` | source | n/a | 2026-08-16 | established |

Property **names** only are recorded. No configuration **value** was read, and none appears in this dossier or in any sibling.

### Build participation

| id | assertion | where | kind | environment | observed | confidence |
|---|---|---|---|---|---|---|
| `code-35` | The project excludes `Services/**` and `Test/**` from compilation | `Diginsight.Tools.FeedMonitor.csproj:8-9` | configuration | n/a | 2026-08-16 | established |
| `code-36` | `src/Job/Diginsight.Tools.FeedMonitor/FeedMonitorBackgroundService.cs` is an empty file outside every project and is **not** part of this component | repository path, no project reference | source | n/a | 2026-08-16 | established |

---

## 🕳️ Gaps

| id | what is not established | why it matters | what would settle it |
|---|---|---|---|
| ~~`code-gap-01`~~ | **Closed 2026-08-16.** Both `CanParse` predicates are now established — `code-37`, `code-38`, `code-39`, `code-40` | — | — |
| `code-gap-02` | The polymorphic write shape produced by the two JSON converters | The stored document shape cannot be described | read both converter bodies and one stored document |
| `code-gap-03` | `NewtonsoftJsonCosmosSerializer` behaviour, including any type-name handling | Deserialisation posture cannot be assessed | read the serializer body |
| `code-gap-04` | `CustomerProvidedKeyConfiguration` and `TransferValidationConfiguration` properties and consumers | Two configuration surfaces are undescribed | read both files and search for consumers |
| `code-gap-05` | `TimeSpanParser`, `CosmosDbHelper` and `DefaultCredentialProvider` are called but defined outside this repository | Their contracts cannot be verified from here | out of scope — they belong to a sibling repository |
| `code-gap-06` | Whether the excluded `Services/**` and `Test/**` folders contain anything | Excluded code may still mislead a reader | list both folders |
| `code-gap-07` | `settings.job` — whether one exists and what schedule it declares | The external trigger behind `code-12` is unidentified | look for a job settings file in the deployment output |
| `code-gap-08` | **No test exists anywhere in this repository** | No assertion here can be corroborated by a test name; every record rests on source reading alone | out of scope for this dossier — established at discovery |
| `code-gap-09` | Which of the two line-number readings behind `code-47` is correct | Four sink call sites and three guard lines were read in separate passes and disagree | a third reading of `FeedMonitorBackgroundService.cs:205-230` in one pass |

---

## 📋 Coverage

| Source-set role | Covered | How |
|---|---|---|
| `composition-root` | ✅ | `Program.cs` read end to end |
| `entry-points` | ✅ | `ExecuteAsync` and its call chain read |
| `domain-model` | ✅ | every type under `Models/` enumerated |
| `options-model` | ✅ | every type under `Configuration/` enumerated by name and property name |
| `persistence-model` | ⚠️ partial | sinks, keys and enablement guards established; stored document shape not — `code-gap-02` |
| `schema-definitions` | ⚠️ partial | model shape established; serialised shape not — `code-gap-03` |
| `test-surface` | ❌ **none exists** | established absence, not an unchecked area — `code-gap-08` |
| `settings-sources` | ➡️ deferred | belongs to the `configuration` area |
| `secret-references` | ➡️ deferred | belongs to the `security` area |
| `infrastructure-definition` · `deployment-descriptor` · `pipeline-definition` · `release-gates` | ➡️ deferred | belong to the `environment` and `devops` areas |
| `authn-authz-surface` · `transport-and-crypto` | ➡️ deferred | belong to the `security` area |

**Not consulted**: any live surface. No process was started, no storage account or database was contacted, and no configuration value was read.

---

## Version history

| Version | Date | Change |
|---|---|---|
| 1.1.0 | 2026-08-16 | Re-investigation prompted by verification findings F-01, F-04, F-10, F-11 and F-12. Added `code-37`–`code-47`: the two `CanParse` predicates and their mutual exclusivity, the neither-claims throw, the factory return types, the HTTP fetch and factory-invocation call sites, the absence of inbound references, and the per-sink `Enabled` guards. Closed `code-gap-01`. Promoted `code-16` from *corroborated* to *established*. Opened `code-gap-09` for a line-number disagreement between two passes. Recorded `code-44`: the registered `HttpClient` is bypassed by a directly constructed one |
| 1.0.0 | 2026-08-16 | Initial investigation at revision `016193f` |
